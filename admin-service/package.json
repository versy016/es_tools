{
  "name": "estools-admin-service",
  "version": "1.0.0",
  "description": "AWS Lambda for ES Tools user management (Cognito admin) + audit log.",
  "main": "index.js",
  "private": true,
  "dependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.600.0",
    "@aws-sdk/client-dynamodb": "^3.600.0"
  }
}
